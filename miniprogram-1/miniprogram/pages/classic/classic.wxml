<v-episode></v-episode>
<v-like
        like="{{latest.like_status}}"
        count="{{latest.fav_nums}}"
        bind:like="onlike"
></v-like>
<v-movie bgImg="{{latest.image}}" contents="{{latest.content}}"></v-movie>
