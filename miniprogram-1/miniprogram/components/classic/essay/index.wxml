<view class="container" hidden="{{hidden}}">
    <image class="classic-item" src="{{bgImg}}"></image>
    <image class="tag" src="./images/essay@tag.png"></image>
    <text class="content">{{contents}}</text>
</view>
